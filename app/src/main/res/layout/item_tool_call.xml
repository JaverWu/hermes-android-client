<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/bg_tool_card"
    android:clipToPadding="false">

    <!-- 左侧强调条（VS Code Copilot 风） -->
    <View
        android:id="@+id/viewAccent"
        android:layout_width="3dp"
        android:layout_height="match_parent"
        android:background="@color/tool_card_accent" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="@dimen/space_md"
        android:paddingTop="@dimen/space_sm"
        android:paddingEnd="@dimen/space_md"
        android:paddingBottom="@dimen/space_sm">

        <!-- 头部：emoji + 标题 + 状态 + 折叠箭头 -->
        <LinearLayout
            android:id="@+id/layoutToolHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="?attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true">

            <TextView
                android:id="@+id/textEmoji"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/textTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="@dimen/space_sm"
                android:textSize="14sp"
                android:textStyle="bold"
                android:singleLine="true"
                android:ellipsize="end"
                android:textColor="@color/tool_card_title" />

            <TextView
                android:id="@+id/textStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_tool_status"
                android:paddingStart="@dimen/space_sm"
                android:paddingEnd="@dimen/space_sm"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:textSize="11sp"
                android:textColor="@color/tool_card_status_text" />

            <ImageView
                android:id="@+id/imageChevron"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="@dimen/space_sm"
                android:src="@drawable/ic_chevron_right"
                android:rotation="90"
                app:tint="@color/text_secondary" />
        </LinearLayout>

        <!-- 预览（折叠时隐藏，点击头部展开） -->
        <TextView
            android:id="@+id/textPreview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_sm"
            android:textSize="12sp"
            android:lineSpacingExtra="2dp"
            android:typeface="monospace"
            android:textColor="@color/tool_card_preview"
            android:maxLines="6"
            android:ellipsize="end" />
    </LinearLayout>
</LinearLayout>
