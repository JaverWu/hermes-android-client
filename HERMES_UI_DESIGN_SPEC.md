# Hermes · 对话界面设计方案（UI Design Spec）

> 参考 Telegram 交互习惯，结合原生质感重新设计的简洁聊天界面。目标：美观、易用、符合直觉。
> 配套可交互原型：`hermes-ui-prototype.html`（含浅色/深色实时切换、标签栏切换、发送微交互）。

---

## 1. 设计原则

| 原则 | 说明 |
|------|------|
| **延续习惯** | 沿用 Telegram 式扁平行列表、左灰右蓝气泡、首字母彩色头像、未读红点——用户无需学习即可上手。 |
| **单一强调色** | 仅用一种品牌蓝（Primary），避免多彩噪音；强调色因"稀有"而有力（60-30-10）。 |
| **冷调中性灰** | 中性色统一偏向品牌色相（chroma 0.01），不出现纯黑/纯白，更自然。 |
| **有节奏的间距** | 4px 基准网格（8/16/24/32/48），间距制造视觉韵律而非平均堆砌。 |
| **克制的卡片** | 列表用发丝线分隔（非重卡片）；卡片仅用于设置分组，降低视觉噪音。 |

---

## 2. 设计 Token（OKLCH）

### 颜色
| Token | 浅色 | 深色 | 角色 |
|-------|------|------|------|
| `--primary` | `oklch(60% 0.20 257)` | `oklch(68% 0.17 257)` | 品牌蓝 / CTA / 发送键 |
| `--bubble-in` | `oklch(95% 0.008 260)` | `oklch(27% 0.016 260)` | 接收气泡（左） |
| `--bubble-out` | = primary | = primary(降饱和) | 发送气泡（右） |
| `--bg` | `oklch(98.5% 0.005 260)` | `oklch(16% 0.012 260)` | 页面背景（非纯黑） |
| `--surface` | `oklch(100% 0 0)` | `oklch(20.5% 0.013 260)` | 卡片/顶栏表面 |
| `--text-1/2/3` | 22% / 45% / 62% 亮度 | 95% / 73% / 56% | 主/次/弱文本 |
| `--unread` | `oklch(58% 0.22 25)` | 同 | 未读红点 |
| `--success` | `oklch(64% 0.16 150)` | 同 | 已连接/成功 |

> 深色模式**非反色**：用表面提亮表达层级，强调色略降饱和，正文字重微调（400→350 观感）。

### 字体
- **族**：`Plus Jakarta Sans`（ distinctive + 高可读性，避开 Inter/Roboto 默认审美），多字重（400–800）建立层级，无需第二字体。
- **字号阶梯**：xs 12 / sm 13 / base 15 / md 17 / lg 20 / xl 26 / 2xl 34。App UI 用固定 rem（非流体），保证容器布局可预测。
- **数字**：时间戳、徽标用 `tabular-nums`，对齐稳定。

### 间距 / 圆角 / 阴影
- 间距：4px 基准 → 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48。
- 圆角：sm 10 / md 14 / lg 20 / xl 28 / full。
- 阴影：3 级 elevation；深色模式下用表面差代替阴影。

---

## 3. 核心界面

### 3.1 会话列表（Chats）
- 顶栏：标题「Hermes」+ 搜索 + 更多。搜索图标点击以**贝塞尔曲线（ease-out-quint）平滑展开**胶囊搜索框，不再生硬切换。
- 列表项：50px 圆形首字母头像（**相邻色渐变** avatar_1~6，见 §3.4）→ 名称 + 时间 → 双行预览 + 未读红点。点击任意项带头像进入对应对话。
- 底部：**玻璃悬浮标签栏**（对话 / ＋FAB / 设置）。

### 3.2 对话界面（Thread）
- 顶栏：返回 + 头像 + 名称 + 副标题「Hermes · 在线」+ 会话设置。
- 气泡：接收左灰、发送右蓝；日期分隔、已读双勾 ✓✓、输入中三点动画。
- 输入条：回形针（唤起斜杠命令面板）+ 文本框 + 蓝色圆形发送 FAB（纸飞机）。
- **神奇缩放（Magic Zoom）**：底部标签栏中间蓝色「＋」按钮点击后，以 macOS 式共享元素动效放大延展，变身为**全新会话**（expo-out 贝塞尔 `cubic-bezier(0.16,1,0.3,1)`，尊重 `prefers-reduced-motion`）。

### 3.3 设置（Settings）
- 资料区（64px 头像 + 名称 + 在线状态）。
- 分组卡片：连接设置 / 外观 / 后台与显示 / 关于，每行图标 + 标签 + 副标题 + 箭头。
- 复用同一玻璃标签栏，激活态高亮「设置」。

### 3.4 头像：相邻色渐变（非单色）
| class | 渐变（OKLCH, 135°） | 观感 |
|-------|----------------------|------|
| av-1 | 18 → 48 | 红 → 橙 |
| av-2 | 150 → 182 | 绿 → 青 |
| av-3 | 250 → 282 | 蓝 → 靛 |
| av-4 | 300 → 332 | 紫 → 品红 |
| av-5 | 72 → 102 | 黄绿 → 绿 |
| av-6 | 188 → 220 | 青 → 蓝 |

按联系人名称哈希取色，保证同一人颜色稳定；相邻色相过渡更柔和、有层次。

---

## 4. 玻璃悬浮标签栏规范
- 四周外边距（16px）、圆角 ≥ 24px、毛玻璃 `backdrop-filter: blur(22px) saturate(160%)`。
- 半透明高亮底 + 细描边 + 软阴影；列表滚动时模糊实时响应。
- 中间「＋」为蓝色 FAB 上浮（margin-top 负偏移），激活态切换高亮。

---

## 5. 无障碍（WCAG AA）
- 正文/背景对比 ≥ 4.5:1，大字 ≥ 3:1。
- 触摸目标 ≥ 44×44px；键盘可聚焦，焦点环清晰。
- 语义 HTML（nav/ul）+ aria-label；不依赖颜色传达信息（红点+数字）。
- 字号用 rem，支持放大至 200% 不破版；动效尊重 `prefers-reduced-motion`。

---

## 6. 与现有工程的衔接
- 沿用既定约定：主色 `#007AFF`（此处以 OKLCH 表达为同观感蓝）、avatar_1~6 调色板、回形针唤起 SlashCommandPanel、顶栏副标题固定「Hermes」、玻璃标签栏已实现。
- 本方案为**界面视觉与交互层提案**，可无缝映射到现有 ChatActivity（Compose）与 Conversations/Settings（XML/View）结构。

---
*UI Designer · 2026-07-14 · 浅色/深色双主题 · Telegram 交互习惯*
